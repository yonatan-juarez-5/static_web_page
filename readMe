# This project creates a static web page using html and css
#asked to not push source into github, can share source upon request
#images/ are used to display throughout the web page and allow
#for smooth scrolling without any distorting

